# Frontend (públicas — vão no bundle). Copie para .env e preencha.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_VAPID_PUBLIC_KEY=

# App roda sem Supabase preenchido: mostra preço ao vivo (Binance) e avisa que o
# painel precisa do banco. Preencha após a etapa 2 (schema aplicado).
